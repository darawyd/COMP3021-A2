name: Bandit Security Scan

on:
    push:
    pull_request:

jobs:
    bandit-security:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

<<<<<<< HEAD
            - name: Run Bandit security scan
              uses: shundor/bandit-action@v0.1
=======
            - name: Set up Python
              uses: actions/setup-python@v5
>>>>>>> 5c1455232a3d820720f90125e28238836c36c577
              with:
                  python-version: "3.10"

            - name: Install Bandit
              run: |
                  pip install bandit

            - name: Run Bandit scan
              run: |
                  bandit -r . -lll -o bandit-report.txt -f txt

            - name: Upload Bandit Report
              uses: actions/upload-artifact@v4
              with:
                  name: bandit-report
                  path: bandit-report.txt
